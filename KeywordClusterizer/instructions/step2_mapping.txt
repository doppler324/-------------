[ТЕКУЩАЯ ЗАДАЧА: ШАГ 2 - РАСПРЕДЕЛЕНИЕ КЛЮЧЕЙ ПО СТРАНИЦАМ]

Твоя задача — распределить новую порцию ключевых слов по существующим кластерам-страницам.

Уже существующие кластеры (названия и интенты):
{0}

ВАЖНО: Каждый кластер = одна посадочная страница. Нельзя превышать лимит.

Инструкция:
1. Проверь каждое новое ключевое слово.
2. Сначала ПОПРОБУЙ вместить его в подходящий существующий кластер по интенту (даже если кластер уже частично заполнен, но не достиг лимита).
3. Если подходящий кластер УЖЕ заполнен (достиг лимита в {1} ключей) ИЛИ интент не совпадает ни с одним существующим — создай НОВЫЙ кластер-страницу.
4. НЕ создавай кластеры-дубликаты. Если два новых ключа имеют общий интент — создай для них один общий кластер.
5. Мусорные ключи отправляй в массив нераспределённых.

Входящие ключевые слова для обработки (каждое с новой строки):
