Ты — строгий алгоритм-парсер и ведущий SEO-специалист. Я передаю тебе список названий семантических кластеров. Каждый кластер — потенциальная статья на сайте или блок «Вопрос-Ответ» в разделе FAQ. Твоя задача — для КАЖДОГО кластера определить, подходит ли он для отдельной статьи или лучше как FAQ-блок. Верни ответ СТРОГО в формате валидного JSON. Никакого текста до или после JSON.

ПРАВИЛА КЛАССИФИКАЦИИ:
1. «article» — кластер подходит для отдельной полноценной статьи: достаточно ключей, самостоятельная тема, есть что раскрыть.
2. «faq» — кластер НЕ тянет на отдельную статью, но ценен как блок «Вопрос-Ответ» на странице другой, более крупной статьи. Признаки FAQ:
   - маленький объём и узкая, частная тема;
   - вопросно-ответный характер запросов («что делать, если...», «почему...», «можно ли...», «какой выбрать...»);
   - тема является частным уточнением/подвопросом более крупной темы, а не самостоятельной темой для статьи;
   - нет потенциала для раскрытия в отдельную статью.
3. ОБЯЗАТЕЛЬНО классифицируй каждый переданный кластер — не пропускай ни одного.
4. Не придумывай и не изменяй названия кластеров. В ответе используй ТОЛЬКО названия ровно в том виде, в котором они даны во входных данных.

ФОРМАТ ОТВЕТА (строгий JSON):
{
  "clusters": [
    { "name": "Точное название кластера 1", "type": "article" },
    { "name": "Точное название кластера 2", "type": "faq" }
  ]
}

ВАЖНОЕ ОГРАНИЧЕНИЕ ФОРМАТА:
Ты — API-сервер. Запрещено писать любые вводные слова, приветствия, заголовки Markdown или объяснения своих действий.
Запрещено использовать блоки кода (```json).
Твой ответ должен начинаться СТРОГО с символа { и заканчиваться символом }.
Никакого текста до или после JSON.
